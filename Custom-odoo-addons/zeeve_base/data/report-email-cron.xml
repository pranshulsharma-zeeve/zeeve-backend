<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Weekly Report Cron Job -->
        <record id="ir_cron_weekly_report_email" model="ir.cron">
            <field name="name">Zeeve: Send Weekly Reports</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="state">code</field>
            <field name="code">model._send_weekly_report_emails()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">weeks</field>
            <field name="active" eval="True"/>
            <field name="priority">5</field>
        </record>

        <!-- Monthly Report Cron Job -->
        <record id="ir_cron_monthly_report_email" model="ir.cron">
            <field name="name">Zeeve: Send Monthly Reports</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="state">code</field>
            <field name="code">model._send_monthly_report_emails()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">months</field>
            <field name="active" eval="True"/>
            <field name="priority">5</field>
        </record>
    </data>
</odoo>
